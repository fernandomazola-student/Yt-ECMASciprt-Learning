/**
 Como na maioria das lnguiagens, quando nao queremos retornar, podemos retornar void
 */

 //Necessariamente, nao precisamos retornar algo, eu mato a função em relação a retorno. Se deixarmos em branco, ela é void tbm
 //mas temos a opção de usar o retorno, nesse caso não.
 void function name(params) {
     
 }